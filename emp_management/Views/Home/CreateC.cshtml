@model CustomerCreateViewModel
@inject ICustomerRep _customerRep

@{
    ViewBag.Title = "Create Customer";
}

<form enctype="multipart/form-data" asp-controller="Customer" asp-action="Create" method="post" class="mt-3">
    <div class="form-group row">
        <label asp-for="Name" class="col-sm-2 col-form-label"></label>
        <div class="col-sm-10">
            <input asp-for="Name" class="form-control" placeholder="Name" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
    </div>
    <div class="form-group row">
        <label asp-for="Email" class="col-sm-2 col-form-label"></label>
        <div class="col-sm-10">
            <input asp-for="Email" class="form-control" placeholder="Email" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>
    </div>

    <div class="form-group row">
        <label asp-for="Photos" class="col-sm-2 col-form-label"></label>
        <div class="col-sm-10">
            <div class="custom-file">
                <input multiple asp-for="Photos" class="form-control custom-file-input" />
                <label class="custom-file-label">Choose File...</label>
            </div>
        </div>
    </div>

    <div asp-validation-summary="All" class="text-danger"></div>
    <div class="form-group row">
        <div class="col-sm-10">
            <button class="btn-primary" type="submit">Create</button>
        </div>
    </div>

    @section Scripts{
        @*<script>
                $(document).ready(fucntion() {
                    $('.custom-file-lable').on("change", function () {
                        var filename = $(this).val().split("\\").pop();
                        $(this).next('.custom-file-lable').html(filename);
                    })
                });
            </script>*@

        <script>
            $(document).ready(function () {
                $('.custom-file-input').on("change", function () {
                    //console.log($(this));
                    var fileLabel = $(this).next('.custom-file-label');
                    console.log("Step1");
                    var files = $(this)[0].files;
                    console.log("Step2");
                    if (files.length > 1) {
                        console.log("Step3");
                        fileLabel.html(files.length + ' files selected');
                        console.log("Step4");
                    }
                    else if (files.length == 1) {
                        console.log("Step5");
                        fileLabel.html(files[0].name);
                        console.log("Step6");
                    }
                    console.log("Step7");
                    //console.log(filename);
                })
            });
        </script>
    }

    @*<div class="form-group row">
            <div class="col-sm-10">
                Total Customer Count = @_customerRep.GetAllCustomer().Count().ToString()
            </div>
        </div>*@
</form>